import pg from 'pg'; const pool = new pg.Pool({ connectionString: process.env.DATABASE_URL }); async function main() { try { const res = await pool.query('DELETE FROM otp_sessions WHERE phone = $1', ['7067381896']); console.log('Success, deleted:', res.rowCount); } catch (e) { console.error('Error:', e); } process.exit(0); } main();
