{
  "version": 2,
  "installCommand": "cd ../.. && npm i -g pnpm && pnpm install",
  "buildCommand": "cd ../.. && pnpm --filter @workspace/db run build && pnpm --filter @workspace/api-zod run build && pnpm --filter @workspace/api-server run build",
  "rewrites": [
    { "source": "/(.*)", "destination": "/api" }
  ]
}
